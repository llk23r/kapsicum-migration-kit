import Foundation
import MigrationKitCLI

let message = """
    migrationkit-cli is a host adapter shell.
    Embed `MigrationCLI.run(arguments:host:)` in your app executable and provide host-specific db opening + migration registry wiring.
    """

print(message)
